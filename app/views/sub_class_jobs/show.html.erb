<%- model_class = SubClassJob -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:name) %>:</strong></dt>
  <dd><%= @sub_class_job.name %></dd>
  <dt><strong><%= model_class.human_attribute_name(:class_job_id) %>:</strong></dt>
  <dd><%= @sub_class_job.class_job_id %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              sub_class_jobs_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_sub_class_job_path(@sub_class_job), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              sub_class_job_path(@sub_class_job),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
